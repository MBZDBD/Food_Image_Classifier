# Food_Image_Classifier
A convolution neural network that classifies images as food according the the classes in the Food11 dataset.  A user interface built in Streamlit allows you to select an image and pass it through the model.  The models prediction can be compared using other tools such as OpenAI and Gemini.
